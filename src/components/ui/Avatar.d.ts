interface AvatarProps {
    src: string;
    alt: string;
    size?: 'sm' | 'md' | 'lg';
    className?: string;
}
export declare function Avatar({ src, alt, size, className }: AvatarProps): any;
export {};
