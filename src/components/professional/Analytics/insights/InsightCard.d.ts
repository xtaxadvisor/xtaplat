import type { AnalyticsInsight } from '../../../../types/analytics';
interface InsightCardProps {
    insight: AnalyticsInsight;
}
export declare function InsightCard({ insight }: InsightCardProps): any;
export {};
